# The source question book is stamped RESTRICTED on every page and this
# repository is public — keep it local. It is not needed at runtime; the app
# only reads data/questions.json.
PDF.pdf

# Raw extraction dump (regenerated by build/1_extract.py from PDF.pdf)
build/questions_raw.json

# THE UNLOCK CODES YOU SELL. Never commit this — anyone with it unlocks
# the app for free. Back it up somewhere private; it cannot be recovered
# from data/unlock.json, which holds only hashes.
build/codes-private.csv

# Local tooling
.claude/
.vercel/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Seeded design-canvas artifact (rebuild with the design skill from design/*.dc.html)
design/kora-utware-redesign.html
design/chrome-profile/
